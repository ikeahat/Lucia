# Lucia
1. Paste prompt and lecture slides into preferred LLM to create .json LIST with cards as objects.

2. Manually add cards to the cards.json file. "front" and "back" need not be capitalised!

3. terminal command is calling python as python, py or python3 + anki_import.py + "DECK NAME"

4. error handling will let you know if anything goes wrong.
